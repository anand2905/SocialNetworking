# Here is a list of your pending requests
<ul>
    <% @users.each do |user| %>
        <li>
            <%= user.name %> |
            <%= link_to "Add Friend", user_friendships_path(:user_id => current_user.id, :friend=> user.id) , :method => post %>
        </li>
    <% end %>
</ul>


